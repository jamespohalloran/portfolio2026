# Frontend build-time vars (Vite). Copy to `.env` for local dev, and set the
# same value in the Pages project → Settings → Variables (Production + Preview).
#
# Public Turnstile site key (the non-secret half of the widget). It is embedded
# in the built HTML, so exposing it is expected and fine.
VITE_TURNSTILE_SITEKEY=
